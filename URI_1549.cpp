#include <iostream>
#include <cmath>
#include <iomanip>

int main(){

  std::ios_base::sync_with_stdio(false);
  std::cin.tie(nullptr);

  int testCases, people, maxRadius, minRadius, height;
  double resultHeight;
  long coke;

  std::cin >> testCases;

  for(int i = 0; i < testCases; i++){

    std::cin >> people >> coke >> minRadius >> maxRadius >> height;

    

  }

  return 0;
}
