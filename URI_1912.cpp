#include <iostream>

int main(){

  std::ios_base::sync_with_stdio(false);
  std::cin.tie(nullptr);

  long qntTiras, area;

  while(std::cin >> qntTiras >> area && qntTiras != 0 && area != 0){}

  return 0;
}
